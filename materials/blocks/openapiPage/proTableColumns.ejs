const columns<%=openApi.schema.title %>: ProColumns<<%=(config.namespace ? config.namespace + ".": "") %><%=openApi.schema.title %>>[] = [
  <% openApi.props.forEach((c, i) => {%>
    {
      title: "<%=c.description %>",
      dataIndex: "<%=c.name %>",
        <% if (c.type == 'boolean') { %>
          valueEnum: new Map([
            [true, '是'],
            [false, '否'],
          ]),
        <% } %>
        <% if (c.format == 'date-time') { %>
          valueType: 'dateTimeRange',
        <% } %>

    },<%});%>
    ];